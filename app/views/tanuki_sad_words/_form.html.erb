<%= form_with(model: tanuki_sad_word, local: true) do |form| %>
  <% if tanuki_sad_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tanuki_sad_word.errors.count, "error") %> prohibited this tanuki_sad_word from being saved:</h2>

      <ul>
        <% tanuki_sad_word.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sad_word %>
    <%= form.text_area :sad_word %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
